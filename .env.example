# PostgreSQL connection string used by the app (Drizzle / postgres-js)
DATABASE_URL=postgres://meal:meal@localhost:5432/meal

# Credentials for the bundled Postgres container (docker-compose.prod.yml)
POSTGRES_USER=meal
POSTGRES_PASSWORD=meal
POSTGRES_DB=meal
